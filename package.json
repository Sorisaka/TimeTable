{
  "name": "live-timetable-ops",
  "version": "0.4.0",
  "description": "ライブのタイムテーブル作成アプリ — 行追加/削除・DnD配置・コンフリクト検出（ドメイン/アプリ層とUI結線）",
  "main": "src/main/main.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "electron .",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "electron": "^31.3.0"
  },
  "engines": {
    "node": ">=18.0.0 <23"
  }
}
